# Get a key at https://mlh.link/gemini or https://aistudio.google.com/apikey
GEMINI_API_KEY=paste_your_key_here

# Optional: pin a model. If unset, the app resolves the best model this key can
# actually use, from MODEL_PREFERENCE in app.py, skipping any that 404 on call.
# GEMINI_MODEL=gemini-3.8-flash
